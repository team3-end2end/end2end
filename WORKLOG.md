# WORKLOG

프로젝트 진행 중 **판단이 바뀐 지점과 그 근거**를 시간순으로 남긴다.
PR 본문이 "이 브랜치에서 내린 결정"을 담는다면, 이 문서는 "프로젝트 전체에서 결정이 어떻게 변해왔는가"를 담는다.
결과가 이상할 때 어느 판단에서 갈라졌는지 추적하기 위한 문서이므로, **번복된 판단도 지우지 않고 남긴다.**

---

## 2026-08-05 — PR #1 리뷰 대응 (판단 번복 1건 포함)

리뷰어가 다른 머신(8코어)에서 원본 데이터를 직접 받아 Step 1~3을 재실행하고 검증한 결과에 대응했다.
`preprocess.json`·`eda.json`·`features.json`은 diff 0으로 완전히 재현됐고, 문제는 아래 3종이었다.

### 번복된 판단 — `VendorID`를 피처로 쓴 것

`03_eda.py`는 `VendorID`·`DOLocationID`를 범주형 피처로 확정하면서, **정작 그 두 컬럼을
타깃과 교차 검토하지 않았다.** 검증 대상은 `pickup_hour`·`day_of_week`·`PULocationID` 셋뿐이었다.

실제로 확인하니 **`VendorID == 6`인 6,614행이 예외 없이 전부 Flex Fare**였다.
원핫 인코딩하면 `VendorID_6` 컬럼이 해당 행의 정답을 그대로 알려주는 누수다.

이것은 **"코드에 컬럼명을 적지 않으면 누수가 섞일 경로가 없다"는 D3 설계의 빈틈**이다.
화이트리스트 방식은 경로를 막아줄 뿐, **화이트리스트에 들어간 컬럼이 검증됐음을 보장하지 않는다.**

→ `VendorID`를 `excluded_leakage`로 이동. `DOLocationID`는 검증 결과 정상이라 유지하되,
   근거가 되는 교차표를 `eda.md`에 남겼다. 이후 **범주형 컬럼은 교차표 검증 후에만 화이트리스트에 넣는다.**

### 서술 오류 — "카드 결제만 팁이 기록된다"

실측은 카드 90.18% / **Flex Fare 9.42%(82,760건)** / 현금 0.01%(25건)다.
팀 리포트 PDF는 맞게 썼고 리포의 `eda.md` 문장이 틀렸다. 원인은 소수 1자리 반올림으로
현금 0.01%가 0.0%로 보인 것. **정확한 서술은 "카드만 팁이 있다"가 아니라 "현금만 팁이 없다"**다.
→ 2자리 반올림 + 건수 병기로 바꾸고 문장을 고쳤다. 덤으로 "팁이 있는데 카드가 아니면 Flex Fare"라는
   식별 경로도 드러났다.

### 측정·문서 정합

- 속도 배수 — 위 "Step 1 완료" 항목의 정정 참조
- `$0.50` 배수 비율 — 손으로 옮긴 값(2.8/27.4/25.4)이 틀렸고 `eda.json`(2.70/27.19/23.66)이 맞았다
- 이상값 규칙의 부등호를 keep 조건의 정확한 부정으로 통일 (경계값 행이 어느 규칙에도 안 잡히던 문제)
- 결측 교차검증이 첫 컬럼 1개만 보면서 문서는 5개 전부 검증한 것처럼 읽히던 문제 → 5개 전부 검사
- `requirements.txt` 버전 핀 추가 — 핀이 없어 리뷰어 환경에서 pandas 1.5.2가 잡혔다
- `is_weekend` 제외 사유를 `excluded_reason`에 명시 (`day_of_week`에서 완전히 유도됨)

### 분업 경계 침범 — Step 5와 Step 3 일부를 반환

`pj.txt` 기준 2-1의 범위는 **결측·중복 처리 / subset / 파생변수 / Pandas·Polars 로딩 비교**
네 가지, 즉 `cleaned.parquet`을 만들어 넘기는 데까지다. 그런데 아래가 함께 들어가 있었다.

| 되돌린 것 | 원 담당 |
|---|---|
| `src/05_visualize.py` + `outputs/figures/*` + `results/figures.json` | 2-2 이은혜 |
| `03_eda.py`의 `describe_overall()` (전체 기술통계) | 2-3 안중범 |
| `03_eda.py`의 `correlation()` (상관계수) | 2-2 이은혜 |

원인은 `plan.md`·`AGENTS.md`를 Step 1~7로 재편하면서 **담당자 이름을 하나도 남기지 않은 것**이다.
소유권이 사람이 아니라 Step 번호 기준이 되니, Step만 채우면 되는 것처럼 보였다.

`pj.txt`의 규칙은 "Merge 후 다음 사람에게 토스 → Loop"인데, **받는 시점에 이미 산출물이 있으면
토스가 성립하지 않는다.** `AGENTS.md`가 스스로 정한 "남의 Step 파일 개선 금지"와도 어긋난다.

→ 위 3건을 삭제하고 `plan.md`에 **담당자 배정 표를 신설**해 `pj.txt`와 연결했다.
   `plan.md`의 해당 요구사항 체크박스도 `[x]` → `[ ]`로 되돌렸다.
   두 항목 모두 `features.json`에 쓰이지 않으므로 Step 6의 입력에는 영향이 없다.

**예외 — `clean()`의 이상값 제거는 유지했다.** `pj.txt`상 2-2의 "이상치 처리"에 해당하지만,
`cleaned.parquet`이 Step 3~6 전체의 입력이라 이것만 빼면 뒤 단계가 이상치 낀 데이터를 받는다.
물리적으로 분리하기 어려운 지점이라 남겨두고 **2-2와 협의 후 조정**하기로 했다.

### 다음 사람에게 넘기는 정보

- **Step 4 (안중범)**: `plan.md` Step 4에 t-검정 2건을 명시해뒀다. 카드 vs 현금 요금은
  p=0.015로 유의하지만 Cohen's d=−0.005로 사실상 차이가 없다. 이것만 제시하면
  "p<0.05이므로 다르다"로 읽혀 정반대 인상을 주므로, Flex Fare vs 카드(d=0.315)와 나란히 놓을 것.
- **Step 6**: `PULocationID` 132번은 **JFK 공항**이다(평균 요금 4배·거리 7배·현금 27%).
  265개 범주를 원핫하는 것보다 `is_airport`(132 JFK, 138 LaGuardia) 파생변수가 설명하기 좋다.

**관련**: PR #1

---

## 2026-08-04 — Step 1 완료: Pandas vs Polars 비교

**한 일**: `src/01_load_compare.py` 작성·실행, 협업 문서(AGENTS.md, PR 템플릿) 세팅.

**측정 결과** (양쪽 모두 `read_parquet` 즉시 실행):

| 항목 | Pandas | Polars | 차이 |
|---|---|---|---|
| 로딩 시간 | — | — | **인용하지 않음** (아래 정정 참조) |
| 메모리 | 581 MB | 550 MB | 1.06배 |
| 전처리(필터+파생변수+집계) | — | — | **약 3~4배** (코어 수에 따라 다름) |

**판단이 바뀐 지점**: 처음에는 "로딩 속도로 라이브러리를 고른다"고 계획했으나, 실제로 재보니
로딩 격차가 선택 근거가 되지 못했다. Pandas의 `read_parquet`도 내부적으로 PyArrow를
쓰기 때문에 parquet 디코딩 비용이 비슷한 것이 원인이다.

→ **Step 2에서 실제로 수행할 전처리 작업으로 다시 측정**했고, 거기서 3~4배 차이를 확인했다.
두 라이브러리의 집계 결과가 동일한 것도 검증했다.

> **정정 (PR #1 리뷰, 2026-08-04)** — 원래 여기에 로딩 1.48배 / 전처리 4.46배로 적었으나,
> 다른 환경에서 재현하니 로딩 배수가 **1.00x~2.56x로 실행마다 요동쳤다.**
> 원인은 측정 루프가 이전 프레임(581MB)을 해제하지 않아 1회차만 조건이 달랐던 것.
> `01_load_compare.py`의 루프를 고치고 `N_RUNS`를 15로 올렸다.
>
> **수정 후 확인**: 같은 머신에서 3회 연속 실행 시 로딩 1.11~1.17배, 전처리 4.31~4.43배로 안정됐다.
> (수정 전 로딩 1.00~2.56배) 리뷰어의 원인 진단이 정확했다.
>
> 다만 **로딩 배수는 환경(코어 수·디스크)에 따라 달라지므로 이후 어떤 문서에도 단일 값으로 적지 않는다.**
> 전처리 배수는 8코어에서 2.7~3.7배, 12코어에서 4.2배 — 방향은 견고하므로
> **"약 3~4배(코어 수에 따라)"로만 표기한다.** 결론(Polars 사용)은 유지된다.

**결론**: Step 2 전처리는 Polars, 저장 직전 `.to_pandas()`로 변환. Step 3 이후는 Pandas
(seaborn·scipy·scikit-learn이 Pandas 객체를 입력으로 받으므로).

**관련**: PR #1

---

## 2026-08-04 — 타깃 클래스 재정의: 이진 분류 → 3-클래스 (판단 번복)

**계기**: 팀 리포트 `payment_type_report.pdf` 검토.

### 번복된 판단

| | 리포트 검토 전 | 검토 후 |
|---|---|---|
| `payment_type=0` (955,371건) | "미상" → 제외 | **Flex Fare** → 포함 |
| 타깃 | 카드 vs 현금 이진 분류 | **신용카드 / 현금 / Flex Fare 3-클래스** |
| 클래스 비율 | 88 : 12 (+ 기타 1%) | **68 : 23 : 9** |

### 왜 틀렸나

`payment_type=0` 행만 `passenger_count`, `RatecodeID`, `store_and_fwd_flag`,
`congestion_surcharge`, `Airport_fee` 5개 컬럼이 **100% 결측**(다른 클래스는 0%)이라,
이를 학습에 넣으면 "승객 수가 비어 있으면 0번"을 외우는 누수가 발생한다고 판단해 제외를 권고했다.

그러나 두 가지를 놓쳤다.

1. **0은 미상이 아니라 Flex Fare다.** NYC TLC 공식 데이터 사전(2025-03-18)에 `0 = Flex Fare trip`으로
   명시돼 있다. 앱 기반 사전 확정요금 제도(2024년 TLC 정식 승인)로, 미터기 세부 요금 필드가
   애초에 존재하지 않기 때문에 비어 있는 것이다. 즉 **결측이 아니라 해당 없음**이다.
2. **누수는 해당 컬럼을 피처에서 빼면 해소된다.** 리포트는 이미 7개 컬럼
   (`tip_amount`, `total_amount`, `congestion_surcharge`, `Airport_fee`, `RatecodeID`,
   `passenger_count`, `store_and_fwd_flag`)을 피처에서 제외해 이 문제를 처리했다.

부수 효과로, 우려하던 클래스 불균형도 개선된다. 대안이었던 카드/현금/기타(무료+분쟁)는
최소 클래스가 0.85%로 사실상 학습 불가였으나, 3-클래스는 최소 클래스가 9%다.

### 유지되는 판단

- `tip_amount`·`total_amount` 제외 (팁 > 0 비율: 카드 90.18% / Flex Fare 9.42% / 현금 0.01%) — 리포트와 동일
- `payment_type` 3(무료)·4(분쟁) 제외 (합산 0.85%) — 리포트와 동일
- 전처리 순서: 대상 선별 → 이상값 제거 → 파생변수 생성

### 리포트 수치 재현 검증

리포트의 데이터 수치를 원본으로 다시 계산해 전부 일치함을 확인했다.

| 항목 | 리포트 | 재현 |
|---|---|---|
| 카드 결제 중 팁 > 0 비율 | 90.2% | 90.2% ✅ |
| 정제 후 신용카드 | 2,660,566 | 2,660,566 ✅ |
| 정제 후 Flex Fare | 878,255 | 878,255 ✅ |
| 정제 후 현금 | 352,974 | 352,974 ✅ |

**출처**: [TLC Yellow Taxi Data Dictionary (2025-03-18)](https://www.nyc.gov/assets/tlc/downloads/pdf/data_dictionary_trip_records_yellow.pdf),
[TLC Flex Fare rule package](https://www.nyc.gov/assets/tlc/downloads/pdf/flex_fare_rule_package_08_14_24.pdf)

---

## 2026-08-04 — 단계 재정비: 전처리 / EDA / 시각화 분리

**계기**: `02_clean_eda.py` 하나가 정제·탐색·시각화를 모두 수행하고 있어,
과제의 어느 요구사항을 어디서 충족했는지 불분명했다.

### 왜 시각화를 뒤로 미뤘나

**차트를 먼저 그리면 해석이 그림에 끌려간다.** 팀 리포트 4.1절이 실증한 사례가 있다.
이상치 제거 전에는 Flex Fare의 평균 거리가 신용카드의 약 3배로 보였으나,
제거 후에는 11% 차이(3.82 vs 3.43마일)였다. 숫자를 먼저 확정하지 않으면
그림이 잘못된 해석을 유도한다.

또한 과제 실습 내용 자체가 ①데이터 준비·EDA ②시각화 ③통계 분석을 **별도 항목**으로 요구한다.
한 파일에 뭉치면 채점자가 어느 요구사항을 어디서 충족했는지 찾기 어렵다.

### 새 구조 — 만든다 → 본다 → 검증한다 → 보여준다

| 단계 | 스크립트 | 성격 | 데이터를 바꾸나 |
|---|---|---|---|
| 2 | `02_preprocess.py` | 만든다 | **바꾼다 (유일)** |
| 3 | `03_eda.py` | 본다 | 안 바꿈 |
| 4 | `04_stats.py` | 검증한다 | 안 바꿈 |
| 5 | `05_visualize.py` | 보여준다 | 안 바꿈 |

이후 번호도 `06_ml_pipeline.py`, `07_report.py`로 조정했다.
**Step 1·2만 존재하는 지금이 번호 조정 비용이 가장 싸다.**

### 함께 도입한 규칙

1. **모든 단계가 JSON + MD를 한 쌍으로 만든다.** JSON은 다음 단계와 `07_report.py`가 읽고,
   MD는 사람이 그 단계만 확인할 때 읽는다. 리포트에 숫자를 직접 타이핑할 일이 없어진다.
2. **누수 차단을 파일로 강제한다.** `03_eda.py`가 `features.json`(수치형 3 + 범주형 5)을 만들고
   `06_ml_pipeline.py`는 그것만 읽는다. 코드에 컬럼명을 적지 않으므로 `tip_amount`가
   실수로 섞일 경로가 없다. 누수 목록 자체도 `preprocess.json`에서 읽어와 단일 기준을 유지한다.
   화이트리스트와 누수 목록의 교집합이 공집합인지 `assert`로 검증한다.
3. **03·04·05는 서로 의존하지 않는다.** 팀원이 나눠 맡거나 동시에 작업해도 된다.

**관련**: PR #1

---

## 2026-08-04 — Step 2·3 완료

**Step 2 (전처리)**: 409만 행 → **3,891,795행**(원본의 95.1%).
클래스 분포 신용카드 68.36% / Flex Fare 22.57% / 현금 9.07%로,
**팀 리포트의 건수와 한 자리도 틀리지 않고 일치**한다(2,660,566 / 878,255 / 352,974).
서로 독립적으로 계산해 같은 값이 나왔으므로 양쪽 다 신뢰할 수 있다.

저장 파일에 남은 결측 4,391,275개가 Flex Fare 878,255행 × 5개 컬럼과 정확히 일치함을
`assert`로 검증한다. 결함이 아니라 '해당 없음'이므로 채우지 않는다.
평균으로 채우면 존재하지 않는 미터기 요금을 지어내는 셈이 된다.

**Step 3 (EDA)**: 여덟 항목 수행. 주요 결과는 다음과 같다.

- **베이스라인 확정**: 최빈 클래스만 예측 시 정확도 **0.6836**, macro F1 **0.2707**.
  Step 6의 판단 기준선이다.
- **상관계수**: 거리–요금 0.862, 거리–소요시간 0.761, 요금–소요시간 0.762
  (팀 리포트 Table 3과 일치). 정보 중복을 뜻하므로 트리 기반 모델 사용 근거가 된다.
- **누수 진단**: 팁 > 0 비율이 신용카드 90.2% / Flex Fare 9.4% / 현금 0.0%.

### 신규 발견 — 132번 구역의 이상 신호

픽업 상위 10개 구역 중 **132번만 결제수단 구성이 확연히 다르다.**

| 구역 | 픽업 건수 | 신용카드 | Flex Fare | 현금 |
|---|---:|---:|---:|---:|
| 237 | 189,939 | 81.86% | 8.19% | 9.95% |
| 236 | 167,782 | 79.85% | 11.70% | 8.45% |
| 161 | 154,409 | 79.47% | 10.33% | 10.20% |
| **132** | **141,188** | **72.70%** | **0.21%** | **27.09%** |
| 142 | 119,283 | 76.17% | 13.78% | 10.05% |

Flex Fare가 사실상 없고(0.21%) 현금이 다른 구역의 세 배다.
픽업 14만 건 규모의 주요 구역이라 우연으로 보기 어렵고, 공항처럼 성격이 다른 지점일 가능성이 있다.
**모델이 지역 정보를 쓸 근거**가 되므로 Step 6에서 확인하고 발표에서 언급할 것.
(PR #1에서 팀원들에게 어떤 구역인지 문의해둠)

**관련**: PR #1

---

## 2026-08-04 — 팀 리포트에서 수정이 필요한 3가지 (미해결)

리포트의 데이터 분석은 재현 검증을 통과했으나, 아래 3건은 최종 제출 전 정정이 필요하다.

### 1. Polars 속도 수치 충돌 — **우선순위 높음**

리포트는 "Polars가 Pandas 대비 약 10~15배 빠른 로딩 속도"라고 기술했으나,
본 측정(양쪽 `read_parquet`)에서는 **로딩 배수가 1.00x~2.56x 사이에서 실행마다 달라졌다.**
10~15배가 나오려면 Polars의 `scan_parquet`(지연 실행, 실제로 읽지 않음)을 사용했거나
워밍업 없이 1회만 측정했을 가능성이 크다.

**같은 프로젝트의 두 문서가 다른 수치를 제시하면 채점에서 신뢰를 잃는다.**
다만 정정 방향은 "다른 숫자로 바꾸기"가 아니라 **"로딩 속도를 근거에서 빼기"**다 —
어떤 값으로 통일해도 다음 사람이 돌리면 또 다른 값이 나오기 때문이다.
전처리 배수는 **"약 3~4배(코어 수에 따라)"**로 표기한다.

### 2. Accuracy 0.48이 베이스라인보다 낮다는 사실이 명시되지 않음

최빈 클래스(신용카드)만 예측하는 베이스라인의 정확도는 0.68인데 모델은 0.48이다.
이는 `class_weight="balanced"`로 소수 클래스 recall을 확보한 대가로 발생한 **의도된 트레이드오프**지만,
리포트는 macro F1(0.441 vs 0.27)만 비교하고 이 사실을 적지 않았다.
과제 요구사항이 "정확도·F1 출력"이므로, 설명 없이 0.48만 제시되면 모델 결함으로 읽힌다.

### 3. 현금 클래스 성능 한계를 더 강하게 다뤄야 함

현금 precision 0.15 — 현금으로 예측한 것 중 15%만 실제 현금이다.
혼동행렬상 실제 신용카드 41,018건 중 13,992건을 현금으로 오분류했다.
리포트도 언급하고 있으나, "실질적 신호가 존재한다"는 결론보다 한계를 더 분명히 밝혀야 한다.

---

## 2026-08-04 — 신규 발견: Flex Fare 요금의 끝자리 패턴

Flex Fare는 사전 확정 요금이라 미터기 요금과 끝자리 분포가 뚜렷이 다르다.

| 결제수단 | 요금이 $0.50 배수인 비율 |
|---|---:|
| Flex Fare | **2.70%** |
| 신용카드 | 27.19% |
| 현금 | 23.66% |

> **정정 (PR #1 리뷰)** — 원래 2.8% / 27.4% / 25.4%로 적었으나, 손으로 옮기면서 틀린 값이었다.
> 위 표는 `outputs/results/eda.json`의 값이다. 현금은 1.7%p 차이라 반올림으로 설명되지 않았다.
> **이후 수치는 JSON에서만 인용한다** (AGENTS.md 코드 규칙 5번).

**의미**: `fare_amount`의 소수점 패턴만으로 Flex Fare가 상당 부분 구분된다.
누수는 아니지만(요금은 정당한 피처), 모델의 Flex Fare 성능(F1 0.58, 3클래스 중 최고)이
이 패턴에 크게 의존할 가능성이 있다. Step 2 EDA에서 확인하고, 발표 시
"모델이 무엇을 근거로 분류하는가"의 설명 재료로 사용할 것.

---

## 2026-08-04 — 리포 설정 변경: ruleset 적용 범위 축소

**문제**: ruleset `push_merge`가 `~ALL` 브랜치를 대상으로 PR을 요구하도록 설정돼 있어,
작업 브랜치에 커밋을 추가 푸시할 수 없었다(브랜치 생성만 가능). 이 때문에 Step 1 작업을
새 브랜치로 옮겨야 했다.

**조치**: 적용 대상을 `~DEFAULT_BRANCH`(main)로 축소. main 보호(삭제 금지·강제푸시 금지·
PR 필수·승인 1명)는 그대로 유지했다. 푸시로 동작을 확인했다.

---

## 열린 질문

- **주제명**: Flex Fare는 엄밀히 "결제수단"이 아니라 "요금 책정 방식"이다(승객이 하차 시 고르는
  카드/현금과 달리, 앱 호출 시점에 결정됨). 타깃이 이질적이므로 "운행 정산 유형 예측"이
  더 정확한 표현이다. 감점 요소는 아니나 발표에서 짚을 가치가 있다.
- 팀 리포트 정정(위 3건)을 누가 언제 반영할 것인가.
