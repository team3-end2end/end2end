# Step 2 — 정제 및 EDA 결과

- 원본: 4,090,836행 × 20컬럼
- 정제 후: **3,891,795행** (원본의 95.1%)

## 1. 정제 전 현황

중복 행은 **0건**으로 제거할 것이 없다.

결측이 있는 컬럼은 아래 5개이며, 모두 정확히 같은 건수다.

| 컬럼 | 결측건수 |
|---|---|
| passenger_count | 955,371 |
| RatecodeID | 955,371 |
| store_and_fwd_flag | 955,371 |
| congestion_surcharge | 955,371 |
| Airport_fee | 955,371 |

**이 결측 행이 전부 `payment_type=0`(Flex Fare)인지 교차 검증한 결과: 일치함.**
따라서 이는 무작위 결측이 아니라, Flex Fare가 앱 기반 사전 확정요금이라
미터기 세부 요금 필드가 **애초에 해당 없음**을 의미한다. 채우거나 지울 대상이 아니다.
다만 이 컬럼들은 Flex Fare에서만 비어 있어 정답을 노출하므로 **모델 피처에서는 제외**한다.

## 2. 정제 내역

### 2-1. 분석 대상 선별
`payment_type` 3(무료)·4(분쟁)를 제외했다. 합산 34,971건으로
표본이 지나치게 적어 신뢰할 수 있는 분류가 불가능하다.

### 2-2. 이상값 제거 (규칙별 해당 건수)

| 규칙 | 해당건수 | 비율% |
|---|---|---|
| 요금 ≤ 0 | 6,087 | 0.15 |
| 이동거리 ≤ 0 | 107,168 | 2.642 |
| 이동거리 > 200마일 | 73 | 0.002 |
| 소요시간 ≤ 0분 | 51,367 | 1.266 |
| 소요시간 > 180분 | 1,404 | 0.035 |

> 규칙끼리 겹칠 수 있으므로 위 합계와 실제 제거 건수는 다르다.
> 대상 선별 후 4,055,865행 → 이상값 제거 후 **3,891,795행**

### 2-3. 생성한 파생변수
`trip_duration_min`(소요시간, 분), `pickup_hour`(승차 시각), `day_of_week`(요일, 월=1),
`is_weekend`(주말 여부), `payment_label`(결제수단 이름)

## 3. 정제 후 클래스 분포

| payment_label | 건수 | 비율% |
|---|---|---|
| 신용카드 | 2,660,566 | 68.36 |
| Flex Fare | 878,255 | 22.57 |
| 현금 | 352,974 | 9.07 |

최소 클래스가 9% 수준이므로 `class_weight="balanced"`로 다룰 수 있다.
다만 최빈 클래스만 예측하는 베이스라인의 정확도가 약 0.6836이므로,
**정확도만으로 모델을 평가해서는 안 되며 macro F1을 함께 봐야 한다.**

## 4. 결제수단별 기술통계

| payment_label | 건수 | 거리평균 | 거리표준편차 | 요금평균 | 요금표준편차 | 소요시간평균 | 소요시간표준편차 |
|---|---|---|---|---|---|---|---|
| 신용카드 | 2,660,566 | 3.43 | 4.46 | 20.27 | 17.75 | 18.89 | 16.71 |
| Flex Fare | 878,255 | 3.82 | 3.36 | 25.59 | 14.12 | 19.26 | 10.72 |
| 현금 | 352,974 | 3.43 | 4.77 | 20.36 | 20.47 | 17.13 | 15.11 |

## 5. Flex Fare 판별 신호 — 요금 끝자리 패턴

| payment_label | $0.50배수비율% |
|---|---|
| Flex Fare | 2.7 |
| 현금 | 23.7 |
| 신용카드 | 27.2 |

미터기 요금은 정해진 단위로 올라가지만 Flex Fare는 사전에 계산된 값이라 끝자리가 고르지 않다.
**요금의 소수점 패턴만으로 Flex Fare가 상당 부분 구분된다**는 뜻이다.
누수는 아니지만(요금은 정당한 피처), Step 4에서 Flex Fare 분류 성능이 높게 나온다면
이 패턴에 의존했을 가능성을 함께 검토해야 한다.

## 6. 시각화

| 차트 | 파일 | 내용 |
|---|---|---|
| Seaborn (정적) | `outputs/figures/seaborn_distance_by_payment.png` | 결제수단별 이동거리 분포 (boxplot) |
| Plotly (인터랙티브) | `outputs/figures/plotly_hourly_payment_mix.html` | 시간대별 결제수단 구성비 (stacked bar) |

Plotly 차트를 비율로 그린 이유: 시간대마다 전체 운행량이 크게 다르므로 건수로 그리면
"새벽에 현금이 적다"가 아니라 "새벽에 운행이 적다"만 보이게 된다.

## 7. 저장 파일에 남은 결측 — 결함이 아님

`cleaned.parquet`에는 결측이 **4,391,275개** 남아 있다.
이는 Flex Fare 878,255행 × 5개 컬럼과
정확히 일치하며, 그 외 행의 결측은 0이다.

채우거나 지우지 않고 그대로 두는 이유는 **값이 없는 것이 아니라 항목 자체가 해당 없기 때문**이다.
평균으로 채우면 존재하지 않는 미터기 요금을 지어내는 셈이 된다.

### 모델 피처에서 제외할 누수 컬럼 7개

`tip_amount`, `total_amount`, `congestion_surcharge`, `Airport_fee`, `RatecodeID`, `passenger_count`, `store_and_fwd_flag`

앞의 두 개는 팁 정보라 결제수단을 그대로 노출하고, 나머지는 Flex Fare에서만 비어 있어
"비어 있으면 Flex Fare"라는 규칙을 외우게 만든다. **Step 4는 반드시 피처 화이트리스트 방식으로
구성해 이 컬럼들이 실수로 포함되지 않게 할 것.**

## 산출물

- `data/processed/cleaned.parquet` — Step 3·4의 입력
- `outputs/results/eda.json` — Step 5의 report.md 생성용 수치
